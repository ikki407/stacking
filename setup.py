from setuptools import setup, find_packages

setup(name='stacking',
      version='0.0.1',
      description='Python stacking for ensemble learning',
      author='ikkitanaka',
      author_email='ikki0407@gmail.com',
      packages=find_packages(),
      )
